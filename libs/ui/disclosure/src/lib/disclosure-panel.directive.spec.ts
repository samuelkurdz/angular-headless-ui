import { DisclosurePanelDirective } from './disclosure-panel.directive';

describe('DisclosurePanelDirective', () => {
  it('should create an instance', () => {
    const directive = new DisclosurePanelDirective();
    expect(directive).toBeTruthy();
  });
});
