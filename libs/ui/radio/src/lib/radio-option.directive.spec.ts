import { RadioOptionDirective } from './radio-option.directive';

describe('RadioOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioOptionDirective();
    expect(directive).toBeTruthy();
  });
});
